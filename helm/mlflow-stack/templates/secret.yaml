apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-mlflow-config
  namespace: {{ .Values.global.namespace }}
type: Opaque
stringData:
  # MLflow-only: no secrets required. Intentionally empty.